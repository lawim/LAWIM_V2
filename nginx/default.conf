# Role
# Skeleton for the future Nginx reverse-proxy configuration of LAWIM_V2.
#
# Usage futur
# This file will describe virtual hosts, upstream routing, headers and health
# probes once the proxy layer is introduced.
#
# Conventions
# - keep application rules out of this file;
# - do not store TLS material or secrets here;
# - keep access and error logging conventions documented separately.
#
# TODO
# - define upstream naming;
# - document security headers and health endpoints;
# - capture the logging hooks expected by later tickets.
