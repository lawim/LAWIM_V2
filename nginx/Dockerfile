# Dockerfile
#
# Role
# Skeleton for the future Nginx image used by LAWIM_V2.
#
# Usage futur
# This file will package the reverse-proxy configuration and the minimum
# assets required for the proxy runtime.
#
# Conventions
# - keep the image surface minimal;
# - do not embed secrets or application code;
# - keep the runtime user and filesystem policy explicit.
#
# TODO
# - choose the base image family;
# - define the copy/install steps for proxy assets;
# - document runtime user and filesystem expectations.
