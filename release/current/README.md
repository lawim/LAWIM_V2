# current

This directory is the working area for a full local release snapshot.

It is intentionally empty in git.

Use it only for generated materialized output from the tracked source tree.

Do not commit build artifacts, archives, or secrets here.

