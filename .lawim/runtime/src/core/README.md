# Core Runtime

The core layer holds the runtime boundary and dispatch model.

- `runtime-contract.md` defines the internal execution contract.
- `dispatch-model.md` maps prepared commands to the services they will eventually call.

## Status

SKELETON_CREATED
