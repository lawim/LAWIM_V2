# Knowledge Graph

The knowledge graph module exposes a lightweight graph representation for topics and hub relationships. It is sufficient for frontend exploration while keeping the backend untouched.
