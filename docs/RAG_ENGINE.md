# RAG Engine

The LAWIM RAG engine supports a mock mode for local testing and a backend mode for future integration. It answers queries using ranked knowledge items and produces answer context with citations.
