# OVH Storage Optimization

The AAC-C storage layer is prepared for OVH migration planning with mock-safe optimization policies:

- hot data kept near active workloads
- cold archives moved to archival storage
- thumbnails retained for faster UI experience
- conversation archives routed to Drive 8
- duplicate storage reduced through a policy object that remains mock-safe
