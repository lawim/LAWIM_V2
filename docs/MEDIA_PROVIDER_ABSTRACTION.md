# Media Provider Abstraction

The provider abstraction introduced for AAC-B remains the foundation for the extended storage platform. It provides a stable interface for local and mock provider implementations without changing existing business APIs.
