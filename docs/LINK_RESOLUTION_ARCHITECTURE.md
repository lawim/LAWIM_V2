# Link resolution architecture

Media business entities resolve to MediaID. The storage platform resolves provider access through the orchestrator and provider-specific temporary links. No business entity stores Google Drive URLs directly.
